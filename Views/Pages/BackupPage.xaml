<Page x:Class="SickReg.Desktop.Views.Pages.BackupPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:vm="clr-namespace:SickReg.Desktop.ViewModels"
      xmlns:models="clr-namespace:SickReg.Desktop.Models"
      d:DataContext="{d:DesignInstance vm:BackupViewModel}"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      Title="Backups"
      Foreground="{DynamicResource TextFillColorPrimaryBrush}">

    <Grid Margin="36,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Backups de Registro" FontSize="28" FontWeight="Bold" />
            <TextBlock Text="Gerencie seus backups de seguranca" FontSize="14" Opacity="0.6" Margin="0,4,0,0" />
        </StackPanel>

        <!-- Actions -->
        <ui:Button Grid.Row="1" Content="Abrir Pasta de Backups" Appearance="Secondary"
                   Icon="{ui:SymbolIcon FolderOpen24}"
                   Margin="0,0,0,16"
                   Command="{Binding OpenBackupFolderCommand}" />

        <!-- No backups -->
        <StackPanel Grid.Row="2" VerticalAlignment="Center" HorizontalAlignment="Center"
                    Visibility="{Binding HasBackups, Converter={StaticResource InverseBoolToVisConverter}}">
            <ui:SymbolIcon Symbol="ArrowDownload24" FontSize="48" Opacity="0.3" HorizontalAlignment="Center" />
            <TextBlock Text="Nenhum backup encontrado" FontSize="16" Opacity="0.5"
                       HorizontalAlignment="Center" Margin="0,12,0,0" />
            <TextBlock Text="Backups sao criados automaticamente antes de cada limpeza"
                       FontSize="13" Opacity="0.4" HorizontalAlignment="Center" Margin="0,4,0,0" />
        </StackPanel>

        <!-- Backup List -->
        <ItemsControl Grid.Row="2" ItemsSource="{Binding Backups}"
                      Visibility="{Binding HasBackups, Converter={StaticResource BoolToVisConverter}}">
            <ItemsControl.ItemTemplate>
                <DataTemplate DataType="{x:Type models:BackupEntry}">
                    <ui:CardControl Margin="0,0,0,8">
                        <ui:CardControl.Header>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <ui:SymbolIcon Grid.Column="0" Symbol="Document24" FontSize="28" Margin="0,0,16,0" Opacity="0.6" />

                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding FileName}" FontSize="14" FontWeight="SemiBold" />
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding CreatedAt, StringFormat='{}{0:dd/MM/yyyy HH:mm}'}" FontSize="12" Opacity="0.6" />
                                        <TextBlock Text=" - " FontSize="12" Opacity="0.6" />
                                        <TextBlock Text="{Binding FileSizeFormatted}" FontSize="12" Opacity="0.6" />
                                    </StackPanel>
                                </StackPanel>

                                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                                    <ui:Button Appearance="Primary"
                                               Icon="{ui:SymbolIcon ArrowUpload24}"
                                               ToolTip="Restaurar"
                                               Margin="0,0,8,0"
                                               Command="{Binding DataContext.RestoreBackupCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                               CommandParameter="{Binding}" />
                                    <ui:Button Appearance="Danger"
                                               Icon="{ui:SymbolIcon Delete24}"
                                               ToolTip="Deletar"
                                               Command="{Binding DataContext.DeleteBackupCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                               CommandParameter="{Binding}" />
                                </StackPanel>
                            </Grid>
                        </ui:CardControl.Header>
                    </ui:CardControl>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</Page>
