<Page x:Class="SickReg.Desktop.Views.Pages.ResultsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:vm="clr-namespace:SickReg.Desktop.ViewModels"
      xmlns:models="clr-namespace:SickReg.Desktop.Models"
      d:DataContext="{d:DesignInstance vm:ResultsViewModel}"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      Title="Resultados"
      Foreground="{DynamicResource TextFillColorPrimaryBrush}">

    <Grid Margin="36,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Resultados do Scan" FontSize="28" FontWeight="Bold" />
            <TextBlock FontSize="14" Opacity="0.6" Margin="0,4,0,0">
                <Run Text="{Binding SelectedCount, Mode=OneWay}" />
                <Run Text=" de " />
                <Run Text="{Binding TotalIssues, Mode=OneWay}" />
                <Run Text=" problemas selecionados" />
            </TextBlock>
        </StackPanel>

        <!-- Action Bar -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,16">
            <ui:Button Content="Selecionar Todos" Appearance="Secondary"
                       Icon="{ui:SymbolIcon CheckboxChecked24}"
                       Margin="0,0,8,0" Command="{Binding SelectAllCommand}" />
            <ui:Button Content="Desmarcar Todos" Appearance="Secondary"
                       Icon="{ui:SymbolIcon CheckboxUnchecked24}"
                       Margin="0,0,8,0" Command="{Binding DeselectAllCommand}" />
        </StackPanel>

        <!-- No results message -->
        <StackPanel Grid.Row="2" VerticalAlignment="Center" HorizontalAlignment="Center"
                    Visibility="{Binding HasResults, Converter={StaticResource InverseBoolToVisConverter}}">
            <ui:SymbolIcon Symbol="DocumentBulletList24" FontSize="48" Opacity="0.3" HorizontalAlignment="Center" />
            <TextBlock Text="Nenhum resultado disponivel" FontSize="16" Opacity="0.5"
                       HorizontalAlignment="Center" Margin="0,12,0,0" />
            <TextBlock Text="Execute um scan para ver os resultados" FontSize="13" Opacity="0.4"
                       HorizontalAlignment="Center" Margin="0,4,0,0" />
        </StackPanel>

        <!-- Results List - Virtualized -->
        <ListView Grid.Row="2"
                  ItemsSource="{Binding Groups}"
                  Visibility="{Binding HasResults, Converter={StaticResource BoolToVisConverter}}"
                  VirtualizingPanel.IsVirtualizing="True"
                  VirtualizingPanel.VirtualizationMode="Recycling"
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                  Background="Transparent"
                  BorderThickness="0">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="Padding" Value="0" />
                    <Setter Property="Margin" Value="0,0,0,8" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListViewItem">
                                <ContentPresenter />
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate DataType="{x:Type vm:ResultsGroupItem}">
                    <ui:CardExpander IsExpanded="{Binding IsExpanded}">
                        <ui:CardExpander.Header>
                            <Grid>
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding DisplayName}" FontSize="15" FontWeight="SemiBold" />
                                    <ui:Badge Appearance="Caution" Margin="12,0,0,0">
                                        <TextBlock Text="{Binding Count}" />
                                    </ui:Badge>
                                </StackPanel>
                            </Grid>
                        </ui:CardExpander.Header>
                        <ui:CardExpander.Content>
                            <ListBox ItemsSource="{Binding Issues}"
                                     MaxHeight="400"
                                     VirtualizingPanel.IsVirtualizing="True"
                                     VirtualizingPanel.VirtualizationMode="Recycling"
                                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                     Background="Transparent"
                                     BorderThickness="0">
                                <ListBox.ItemContainerStyle>
                                    <Style TargetType="ListBoxItem">
                                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                        <Setter Property="Padding" Value="0" />
                                        <Setter Property="Margin" Value="0" />
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="ListBoxItem">
                                                    <ContentPresenter />
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </ListBox.ItemContainerStyle>
                                <ListBox.ItemTemplate>
                                    <DataTemplate DataType="{x:Type models:RegistryIssue}">
                                        <Border Padding="8,6" BorderThickness="0,0,0,1"
                                                BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto" />
                                                    <ColumnDefinition Width="*" />
                                                </Grid.ColumnDefinitions>
                                                <CheckBox Grid.Column="0" IsChecked="{Binding IsSelected}" VerticalAlignment="Center" Margin="0,0,12,0" />
                                                <StackPanel Grid.Column="1">
                                                    <TextBlock Text="{Binding Description}" FontSize="13" TextTrimming="CharacterEllipsis" />
                                                    <TextBlock Text="{Binding FullKeyPath}" FontSize="11" Opacity="0.5" TextTrimming="CharacterEllipsis" Margin="0,2,0,0" />
                                                </StackPanel>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </ui:CardExpander.Content>
                    </ui:CardExpander>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- Clean Button -->
        <Border Grid.Row="3" Padding="0,16,0,0"
                Visibility="{Binding HasResults, Converter={StaticResource BoolToVisConverter}}">
            <ui:Button Content="{Binding IsCleaning, Converter={StaticResource CleanButtonTextConverter}}"
                       Appearance="Danger"
                       Icon="{ui:SymbolIcon Delete24}"
                       FontSize="15"
                       Padding="28,12"
                       HorizontalAlignment="Right"
                       IsEnabled="{Binding IsCleaning, Converter={StaticResource InverseBoolConverter}}"
                       Command="{Binding CleanSelectedCommand}" />
        </Border>
    </Grid>
</Page>
